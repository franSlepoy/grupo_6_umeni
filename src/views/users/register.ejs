<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Register</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter&display=swap" rel="stylesheet">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/images/umeni.jpeg">
    <link rel="stylesheet" href="/css/register.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/footer.css">
    <script src="https://kit.fontawesome.com/851c995492.js" crossorigin="anonymous"></script>
</head>
<body>

  <header>
    <%- include('../partials/header.ejs') %>
  </header>
<br>  

<form method="POST" action="/users/register" enctype="multipart/form-data">
  <!-- <% if (locals.errors) { %>
    <% for (let oneError in errors)  { %>
      <p><%= errors[oneError].msg %></p>
    <% } %>
  <% } %> -->
  <h1>Crea tu cuenta</h1>
  <h4 class=subtitulo>Llena la información requerida</h4>
  <br>
  <label for="name">Nombre Completo:</label>
  <input 
  type="name" 
  placeholder="Ingresa tu nombre completo" 
  id="name" 
  name="name"
  value="<%=locals.oldData ? oldData.name : null %>"
  <br>
  <% if (locals.errors && errors.name) { %>
    <div class="text-danger">
      <%= errors.name.msg %>
    </div>
  <% } %>
  <br>
  <label for="email">Correo Electrónico:</label>
  <input type="email" placeholder="Ingresa tu correo electrónico" id="email" name="email"
  value="<%=locals.oldData ? oldData.email : null %>">
  <br>
  <% if (locals.errors && errors.email) { %>
    <div class="text-danger">
      <%= errors.email.msg %>
    </div>
  <% } %>
  <br>
  <div>
    <label><b>Imagen de perfil: </b></label>
    <br>
    <input type="file" name="avatar">
  </div>
  <br>
  <label for="password">Contraseña:</label>
  <input type="password" placeholder="Ingresa tu contraseña" id="password" name="password"
  <br>
  <% if (locals.errors && errors.password) { %>
    <div class="text-danger">
      <%= errors.password.msg %>
    </div>
  <% } %>
  <br><br>
    <button class="login" type="submit">Registrate</button>
  <br><br>
<button class="google" type="submit"><img src="/images/logos_google-icon.png" alt="" hspace="12px">Registrate con Google</button>
<button class="facebook" type="submit"><img src="/images/logos_facebook.png" alt="" hspace="12px">Registrate con Facebook</button>
  <br><br>
  <div class="login-signup-now">
    ¿Ya te registraste?
    <a href="#" style="text-indent: 10px;">Inicia Sesión</a>
  </div>
 
  <br><br>
</form>
<footer>
  <%- include('../partials/footer.ejs') %>
</footer>
</body>
</html>